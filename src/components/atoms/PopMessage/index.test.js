// import React from 'react';
// import { snapshot } from 'test/helpers';
// import PopMessage from '.';

// const props = {
//   anchorEl: document.body,
//   handleClose: jest.fn(),
//   children: 'メッセージ',
// };

// snapshot('PopMessage/nomal', <PopMessage {...props} />);
test('test', () => expect(true).toBe(true));
